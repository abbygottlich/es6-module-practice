import multiply from "./multiply.js";

export function multiplyTen(num) {
    return multiply(num, 10);
}

export function multiplyFive(num) {
    return multiply(num, 5);
}

export function multiplyOne(num) {
    return multiply(num, 1);
}